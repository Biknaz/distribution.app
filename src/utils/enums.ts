export enum RoleType {
  Admin = 1,
  User,
}

export enum SlideoverModes {
  edit = "Edit",
  show = "Show",
  create = "Create",
  none = "None",
}

export enum THead {
  edit = "Edit",
}

export enum OrderStatus {
  NoPaid = 1,
  InProcess,
  Paid,
  Procesed,
}
